[build]
  command = "pnpm build"
  publish = "dist"

[[edge_functions]]
  function = "api-proxy"
  path = "/api/*"

# SPA fallback: all routes serve index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

# For local development with Vite proxy, no env vars are needed.
# Vite proxies /api/* to http://localhost:8000 automatically.

# --- Netlify production environment variables ---
# Set these in the Netlify dashboard (Site settings > Environment variables),
# NOT in a .env file. They are used by the edge function, not by Vite.
#
# API_URL=https://your-app.railway.app   # Railway backend URL (no trailing slash)
# API_KEY=your-shared-secret             # Shared API key for backend auth
# APP_PASSWORD=your-chat-password        # Password users must enter to use the AI chat

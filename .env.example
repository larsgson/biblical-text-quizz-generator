# API base URL (without trailing slash)
# For local development with Vite proxy, leave empty or omit:
# VITE_API_URL=
# For production, set to the deployed API URL:
# VITE_API_URL=https://text-fabric-mcp.fly.dev
